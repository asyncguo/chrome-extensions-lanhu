import { useState } from "react"

function IndexPopup() {
  const [data, setData] = useState("")

  return (
    <div
      style={{
        width: 200,
        display: "flex",
        flexDirection: "column",
        padding: 16
      }}>
      lanhu plugins 12
    </div>
  )
}

export default IndexPopup
